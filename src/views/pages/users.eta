<% layout('/layouts/base') %>

<div class="page-header">
  <h1>Users</h1>
  <div class="action-row">
    <a href="/users/new" class="btn btn--primary">+ Add user</a>
  </div>
</div>

<div class="table-wrap">
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Role</th>
      </tr>
    </thead>
    <tbody>
      <% it.users.forEach(function(u) { %>
      <tr>
        <td data-label="Name"><%= u.name %></td>
        <td data-label="Email"><%= u.email %></td>
        <td data-label="Role"><span class="condition--<%= u.role === 'admin' ? 'new' : u.role === 'manager' ? 'good' : u.role === 'staff' ? 'fair' : 'poor' %>"><%= u.role %></span></td>
      </tr>
      <% }) %>
    </tbody>
  </table>
</div>
